<style>
    th,td{
        text-align:center;
    }
</style>


<table class="table">
<h1>List of students</h1>
<%= notice%>
<%= link_to 'New Student', new_student_path,  class: "btn btn-primary"%>
<%
=begin%>
 <table style ="border : 2px solid black" class=text-center> 
<%
=end%>
  <thead>
    <tr>
      <th scope="col">sr.no</th>
      <th scope="col">first_name</th>
      <th scope="col">last_name</th>
      <th scope="col">email</th>
      <th scope="col" > Action</th>
    </tr>
  </thead>
  <tbody>
 <% @students.each do|student| %>
    <tr>
      <th scope="row"><%= student.id%></th>
      <td><%= student.f_name%></td>
      <td><%= student.last_name %></td>
      <td><%= student.email %></td>
      <td ><%= link_to 'view',student_path(student), class: "badge bg-primary"%>
      <%= link_to 'edit',edit_student_path(student), class: "badge bg-info"%>
      <%= link_to 'Delete',student,data:{turbo_method: :delete ,turbo_confirm:'Are you sure'}, class: "badge bg-danger"%></td>
    </tr>
<% end %>
    </tr>
  </tbody>
</table>
<%=paginate @students %>